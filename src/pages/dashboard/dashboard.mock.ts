import { MOCK_STATS } from "@/shared/mocks/stats.mock";
import { MOCK_USER_AUTHENTICATED } from "@/shared/mocks/user.mock";

export const MOCK_DASHBOARD = {
  progress: 40,
  user: MOCK_USER_AUTHENTICATED,
  stats: MOCK_STATS.stats,
};
